import styled from 'styled-components';

import {
    colorProps,
    fontProps,
    marginProps,
    paddingProps,
    sizeProps,
} from '@styles/styleProps';

export default styled.p`
  ${sizeProps}
  ${fontProps}
  ${colorProps}
  ${marginProps}
  ${paddingProps}
`;